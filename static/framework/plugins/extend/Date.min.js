Date.prototype.format = function(a) {
    if ("Invalid Date" == this.toString()) return "";
    var b = {
        "M+": this.getMonth() + 1,
        "d+": this.getDate(),
        "h+": this.getHours(),
        "m+": this.getMinutes(),
        "s+": this.getSeconds(),
        "q+": Math.floor((this.getMonth() + 3) / 3),
        S: this.getMilliseconds()
    };
    /(y+)/.test(a) && (a = a.replace(RegExp.$1, (this.getFullYear() + "").substr(4 - RegExp.$1.length)));
    for (var c in b) new RegExp("(" + c + ")").test(a) && (a = a.replace(RegExp.$1, 1 == RegExp.$1.length ? b[c] : ("00" + b[c]).substr(("" + b[c]).length)));
    return a;
}, Date.prototype.getDiff = function(a, b) {
    if ("Invalid Date" == this.toString()) return 0;
    a = new Date(a.toString()), b = b.toLowerCase();
    var c = 1;
    switch (b) {
      case "second":
        c = 1e3;
        break;

      case "minute":
        c = 6e4;
        break;

      case "hour":
        c = 36e5;
        break;

      case "day":
        c = 864e5;
        break;

      case "year":
        c = 31536e6;
        var d = this.getFullYear() - a.getFullYear() - 1;
        return (this.getMonth() > a.getMonth() || this.getMonth() == a.getMonth() && this.getDate() >= a.getDate()) && (d += 1), 
        d;
    }
    return parseInt((this.getTime() - a.getTime()) / parseInt(c));
}, Date.prototype.addDays = function(a) {
    return this.setDate(this.getDate() + a), this;
}, Date.prototype.addMonths = function(a) {
    return this.setMonth(this.getMonth() + a), this;
}, Date.prototype.getTimestamp = function(a) {
    a && this.addDays(a);
    var b = 60 * this.getTimezoneOffset() * 1e3;
    return this.getTime() - b;
}, String.prototype.toDate = function() {
    var a = /^(\d{4})[-|\/](\d{2})[-|\/](\d{2}).*$/, b = this;
    if (a.test(b)) {
        var c = /[+-]{1}\d{2}:\d{2}/gi;
        b = b.replace(/\//g, "-").replace(c, ""), b.indexOf("T") > -1 ? b += "+08:00" : b.indexOf(" ") > -1 && (b = b.replace(" ", "T") + "+08:00"), 
        b = b.replace("T0:", "T00:");
    }
    return new Date(b);
};
//# sourceMappingURL=Date.min.js.map
/*! 康美网络医院 最后修改于： 2016-10-20 */